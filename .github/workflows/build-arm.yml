name: Build ARM Native Image

on:
  push:
    branches:
      - master
    paths:
      - 'pom.xml'

jobs:
  build-arm:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check if version changed
        id: version_check
        run: |
          CURRENT_VERSION=$(xmllint --xpath "/*[local-name()='project']/*[local-name()='version']/text()" pom.xml)

          # Fetch minimal history to get previous pom.xml
          git fetch origin master --depth=2
          git show HEAD^:pom.xml > pom_old.xml || true
          PREVIOUS_VERSION=$(xmllint --xpath "/*[local-name()='project']/*[local-name()='version']/text()" pom_old.xml || echo "")

          echo "Current version: $CURRENT_VERSION"
          echo "Previous version: $PREVIOUS_VERSION"

          # Always set version for later steps
          echo "version=$CURRENT_VERSION" >> $GITHUB_ENV

          if [ "$CURRENT_VERSION" = "$PREVIOUS_VERSION" ]; then
            echo "changed=false" >> $GITHUB_ENV
            echo "No version change detected — skipping build."
          else
            echo "changed=true" >> $GITHUB_ENV
          fi

      - name: Set up GraalVM JDK
        if: env.changed == 'true'
        run: |
          curl -s "https://get.sdkman.io" | bash
          source "$HOME/.sdkman/bin/sdkman-init.sh"
          sdk install java 22.3.r17-grl
          sdk use java 22.3.r17-grl
          java -version
          gu install native-image

      - name: Set up Maven
        if: env.changed == 'true'
        run: |
          brew install maven
          mvn -version

      - name: Build ARM native image
        if: env.changed == 'true'
        run: |
          mvn -PnativeARM -DskipTests native:compile
          tar -czvf snapzap-${{ env.version }}-macos-arm64.tar.gz -C target native-snapzap
          zip -j snapzap-${{ env.version }}-macos-arm64.zip target/native-snapzap

      - name: Create GitHub release (draft if not exists)
        if: env.changed == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.version }}
          name: SnapZap ${{ env.version }}
          draft: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload ARM artifacts to release
        if: env.changed == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.version }}
          files: |
            snapzap-${{ env.version }}-macos-arm64.tar.gz
            snapzap-${{ env.version }}-macos-arm64.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
